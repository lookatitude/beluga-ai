# Wiki Scan Artifact — 2026-04-11

Full codebase scan of Beluga AI v2 Go architecture.

## Scan Scope

- **Date:** 2026-04-11
- **Codebase:** /home/miguelp/Projects/lookatitude/beluga-ai
- **Pattern Count:** 8
- **Packages:** 7 core
- **Invariants:** 10

## Patterns Extracted

### 1. Provider Registration
**Location:** `llm/registry.go:19-27`

```go
func Register(name string, factory Factory) error {
	mu.Lock()
	defer mu.Unlock()
	
	if _, exists := providers[name]; exists {
		return fmt.Errorf("provider %q already registered", name)
	}
	providers[name] = factory
	return nil
}
```

**Variations:**
- `llm/providers/anthropic/anthropic.go:19-23` — init() registration
- `tool/registry.go:17-35` — Instance-based Registry pattern

**Anti-Patterns Found:**
- Using init() with panic() instead of error return
- No cleanup/deregistration function
- Missing duplicate registration guard (potential overwrite)

---

### 2. Middleware
**Location:** `tool/middleware.go:11-22`

```go
func ApplyMiddleware(tool Tool, mws ...Middleware) Tool {
	result := tool
	for i := len(mws) - 1; i >= 0; i-- {
		result = mws[i](result)
	}
	return result
}
```

**Variations:**
- `tool/middleware.go:27-56` — WithTimeout concrete middleware
- `tool/middleware.go:58-85` — WithRetry with exponential backoff

**Anti-Patterns Found:**
- Middleware not preserving metadata (Name, Description)
- Missing context cancellation handling in some wrappers
- Unbounded retry attempts without max limit

---

### 3. Hooks
**Location:** `tool/hooks.go:9-44`

```go
type Hooks struct {
	OnStart func(ctx context.Context, name string, input map[string]any) error
	OnEnd   func(ctx context.Context, name string) error
	OnError func(ctx context.Context, name string, err error) error
}

func ComposeHooks(hks ...Hooks) Hooks {
	return Hooks{
		OnStart: func(ctx context.Context, name string, input map[string]any) error {
			for _, h := range hks {
				if h.OnStart != nil {
					if err := h.OnStart(ctx, name, input); err != nil {
						return err
					}
				}
			}
			return nil
		},
		// ...
	}
}
```

**Variations:**
- Single hook invocation with nil checks
- Guard hooks for Input/Output/Tool stages

**Anti-Patterns Found:**
- Direct hook invocation without nil check (nil dereference risk)
- Hook composition with unbounded depth
- Hooks modifying context/input (breaks idempotence)

---

### 4. Streaming
**Location:** `core/stream.go:49-56`

```go
type Stream[T any] struct {
	name   string
	chunks iter.Seq2[int, T]
}

func (s *Stream[T]) Range(yield func(int, T) bool) {
	for idx, chunk := range s.chunks {
		if !yield(idx, chunk) {
			break
		}
	}
}
```

**Variations:**
- `core/stream.go:73-90` — MapStream producer with transformation
- iter.Seq[T] (single-value iteration) for simpler cases

**Anti-Patterns Found:**
- Buffered channels causing backpressure issues
- Goroutine leaks on early yield return false
- Producer not respecting context cancellation

---

### 5. Testing
**Location:** `tool/tool_test.go:11-39`

```go
func TestTool(t *testing.T) {
	tests := []struct {
		name    string
		tool    Tool
		input   map[string]any
		wantErr bool
		check   func(t *testing.T, result *Result)
	}{
		{
			name: "success",
			tool: &mockTool{executeFn: func(_ map[string]any) (*Result, error) {
				return TextResult("ok"), nil
			}},
			wantErr: false,
		},
	}
	
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			result, err := tt.tool.Execute(context.Background(), tt.input)
			if (err != nil) != tt.wantErr {
				t.Fatalf("wantErr=%v, got err=%v", tt.wantErr, err)
			}
			if tt.check != nil {
				tt.check(t, result)
			}
		})
	}
}
```

**Variations:**
- `tool/middleware_test.go:92-118` — Retry logic validation
- `tool/middleware_test.go:164-188` — Context cancellation tests

**Anti-Patterns Found:**
- Monolithic test functions (no subtests)
- Missing error code (ErrorCode) validation
- Hardcoded sleep durations (flaky tests)

---

### 6. OTel Instrumentation
**Location:** `o11y/tracer.go:15-47`

```go
const (
	AttrAgentName = "gen_ai.agent.name"
	AttrOperationName = "gen_ai.operation.name"
	AttrToolName = "gen_ai.tool.name"
	AttrRequestModel = "gen_ai.request.model"
	AttrResponseModel = "gen_ai.response.model"
	AttrInputTokens = "gen_ai.usage.input_tokens"
	AttrOutputTokens = "gen_ai.usage.output_tokens"
	AttrSystem = "gen_ai.system"
	AttrReasoningTokens = "gen_ai.usage.reasoning_tokens"
	AttrReasoningEffort = "gen_ai.request.reasoning_effort"
)
```

**Variations:**
- `o11y/tracer.go:118-121` — StartSpan with attributes
- Status mapping: `o11y/tracer.go:100-107`

**Anti-Patterns Found:**
- Custom attribute keys (non-standard gen_ai.* naming)
- Missing token count attributes
- Span not ended in defer (resource leaks)

---

### 7. Error Handling
**Location:** `core/errors.go:8-110`

```go
type ErrorCode string

const (
	ErrRateLimit ErrorCode = "rate_limit"
	ErrAuth ErrorCode = "auth_error"
	ErrTimeout ErrorCode = "timeout"
	ErrInvalidInput ErrorCode = "invalid_input"
	ErrToolFailed ErrorCode = "tool_failed"
	ErrProviderDown ErrorCode = "provider_unavailable"
	ErrGuardBlocked ErrorCode = "guard_blocked"
	ErrBudgetExhausted ErrorCode = "budget_exhausted"
	ErrNotFound ErrorCode = "not_found"
)

var retryableCodes = map[ErrorCode]bool{
	ErrRateLimit: true,
	ErrTimeout: true,
	ErrProviderDown: true,
}

func IsRetryable(err error) bool {
	var e *Error
	if errors.As(err, &e) {
		return retryableCodes[e.Code]
	}
	return false
}
```

**Variations:**
- `core/errors.go:66-73` — NewError constructor
- `core/errors.go:77-82` — Error string formatting

**Anti-Patterns Found:**
- Unclassified generic errors (missing ErrorCode)
- Error string checking instead of code enum
- Silent error swallowing (no IsRetryable check)

---

### 8. Security Guards
**Location:** `guard/guard.go:1-52`

```go
type Guard interface {
	InspectInput(ctx context.Context, input GuardInput) (GuardResult, error)
	InspectOutput(ctx context.Context, output GuardOutput) (GuardResult, error)
	InspectTool(ctx context.Context, tool GuardTool) (GuardResult, error)
}

type GuardResult struct {
	Decision Decision
	Reason   string
}

type Decision string

const (
	Allow   Decision = "allow"
	Review  Decision = "review"
	Block   Decision = "block"
)
```

**Variations:**
- Multiple guards chained via pipeline
- Custom guard implementations (budget, content policy, tool access)

**Anti-Patterns Found:**
- Skipping guard stages (incomplete coverage)
- Ignoring Review decisions (treats as Allow)
- Guard errors not propagated
- Unbounded guard chain (O(n) latency)

---

## Packages Scanned

1. **core** — Stream, Error, ErrorCode
2. **tool** — Tool interface, Registry, Middleware, Hooks
3. **llm** — Provider abstraction, registration, Client
4. **guard** — Guard interface, GuardResult, Decision enum
5. **o11y** — OpenTelemetry Span, Attrs, StatusCode
6. **memory** — MessageStore abstraction, in-memory implementation
7. **protocol** — Wire protocol definitions

## Invariants Identified

1. Error Classification via ErrorCode
2. Retryable Code Classification (only rate_limit, timeout, provider_unavailable)
3. Middleware Application Order (reverse iteration)
4. Hooks Are Optional and Nil-Safe
5. Registry Registration Before main()
6. Stream Respects Backpressure
7. Context Cancellation Stops Retries
8. GenAI Attributes Use Standard Prefixes
9. Guard Pipeline Runs All Stages
10. Test Subtests Isolate Failures

---

## Scan Completed

All 8 patterns documented with:
- Canonical examples (file:line)
- 2-3 variations per pattern
- Anti-patterns identified
- Real code snippets
- Invariant references

All 7 packages mapped with:
- Purpose and key types
- Registry status
- Dependencies
- Canonical files
- Test coverage

All 10 invariants documented with:
- File:line references
- Violation symptoms
- Rationale

**Output:** `.wiki/patterns/`, `.wiki/architecture/`, `.wiki/index.md`
