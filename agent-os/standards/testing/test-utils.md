# test_utils.go

**Contents:** Mocks (e.g. `AdvancedMockXxx`), `NewAdvancedMockXxx` constructors, and helpers used by `*_test.go` and `advanced_test.go`.

**Scope:** Only imported by `*_test.go` (and equivalent test files). Do not import from production code.

**Coverage exclusions:** Document exclusions in a separate file or location, not in `test_utils.go`.
