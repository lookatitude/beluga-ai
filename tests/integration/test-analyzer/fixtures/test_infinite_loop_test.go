package fixtures

import (
	"context"
	"testing"
	"time"
)

// TestInfiniteLoop demonstrates an infinite loop pattern for test analyzer
// This test is skipped because it intentionally runs forever.
func TestInfiniteLoop(t *testing.T) {
	t.Skip("Skipping infinite loop test - this is a fixture file that demonstrates the pattern for test analyzer")
	ctx := context.Background()
	for {
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		select {
		case <-ctx.Done():
			return
		case <-time.After(5 * time.Second):
			return
		default:
			// Loop body
		}
		// This is an infinite loop without exit condition
	}
}
