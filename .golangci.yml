# golangci-lint v2 configuration for Beluga AI Framework
# Comprehensive linting setup for production-ready Go framework

version: "2"

run:
  # Timeout for analysis
  timeout: 5m
  
  # Include test files in linting
  tests: true

linters:
  # Enable comprehensive set of linters
  enable:
    # Error checking
    - errcheck
    - errorlint
    - errname
    
    # Security
    - gosec
    
    # Code quality
    - govet
    - staticcheck
    - gocritic
    - revive
    - ineffassign
    - unused
    - unparam
    - unconvert
    
    # Style
    - misspell
    - goconst
    
    # Complexity
    - gocyclo
    - funlen
    - lll
    
    # Duplication
    - dupl
    
    # Documentation
    - godot
    - godox
    
    # Performance
    - prealloc
    - makezero
    
    # Best practices
    - exhaustive
    - forbidigo
    - nolintlint
    - contextcheck
    - containedctx
    - importas
    - nakedret
    - noctx
    - rowserrcheck
    - sqlclosecheck
    - thelper
    - wastedassign

issues:
  # Maximum issues count per one linter
  # Increased significantly to allow typecheck false positives
  # These are false positives - code compiles successfully (go build passes)
  max-issues-per-linter: 500
  max-same-issues: 500