package markdown

// init is intentionally empty - registration is handled in textsplitters/registry_init.go
// to avoid import cycles.
func init() {
	// Registration happens in parent package's registry_init.go
}
